services:
  autobrr:
    image: ghcr.io/autobrr/autobrr:v1.79.0
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:7474:7474"]
    volumes:
      - ${NOOK_DATA}/apps/autobrr/config:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
