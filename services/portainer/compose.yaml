services:
  portainer:
    image: portainer/portainer-ce:latest
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:9443:9000"]
    volumes:
      - ${NOOK_DATA}/apps/portainer:/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      TZ: ${TZ:-UTC}
