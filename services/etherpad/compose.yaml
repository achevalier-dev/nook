services:
  etherpad:
    image: etherpad/etherpad:3.1.0
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:9001:9001"]
    volumes:
      - ${NOOK_DATA}/apps/etherpad/var:/opt/etherpad-lite/var
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
