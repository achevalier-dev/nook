services:
  openlist:
    image: openlistteam/openlist:v4.2.2
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:5245:5244"]
    volumes:
      - ${NOOK_DATA}/apps/openlist/data:/opt/openlist/data
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
