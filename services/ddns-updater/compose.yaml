services:
  ddns-updater:
    image: qmcgaw/ddns-updater:v2.10.0
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8002:8000"]
    volumes:
      - ${NOOK_DATA}/apps/ddns-updater/data:/updater/data
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
