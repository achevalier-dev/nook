services:
  embystat:
    image: linuxserver/embystat:0.2.0
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:6555:6555"]
    volumes:
      - ${NOOK_DATA}/apps/embystat/config:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
