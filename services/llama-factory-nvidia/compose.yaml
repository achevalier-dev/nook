services:
  llama-factory-nvidia:
    image: hiyouga/llamafactory:0.9.4
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:18877:7860"]
    volumes:
      - ${NOOK_DATA}/apps/llama-factory-nvidia:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
