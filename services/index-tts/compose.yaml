services:
  index-tts:
    image: icewhaletech/index-tts:2.0
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:17869:7860"]
    volumes:
      - ${NOOK_DATA}/apps/index-tts:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
