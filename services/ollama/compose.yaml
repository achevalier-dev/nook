services:
  ollama:
    image: ollama/ollama:0.24.0
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:11434:11434"]
    volumes:
      - ${NOOK_DATA}/apps/ollama/:/root/.ollama
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
