services:
  jenkins:
    image: jenkins/jenkins:lts-jdk17
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8080:8080"]
    volumes:
      - ${NOOK_DATA}/apps/jenkins/data:/data
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
