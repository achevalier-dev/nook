services:
  logseq:
    image: correctroad/logseq:v0.10.10
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:3325:3000"]
    volumes:
      - ${NOOK_DATA}/apps/logseq/config:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
