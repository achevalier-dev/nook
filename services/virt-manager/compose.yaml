services:
  virt-manager:
    image: mber5/virt-manager:latest
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8185:80"]
    volumes:
      - ${NOOK_DATA}/apps/virt-manager:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
