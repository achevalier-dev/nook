services:
  hermes:
    image: nousresearch/hermes-agent:v2026.6.5
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:9119:9119"]
    volumes:
      - ${NOOK_DATA}/apps/hermes/:/opt/data
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
