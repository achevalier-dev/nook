services:
  anaconda3:
    image: continuumio/anaconda3:2024.10-1
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8889:8888"]
    volumes:
      - ${NOOK_DATA}/apps/anaconda3/notebooks:/opt/notebooks
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
