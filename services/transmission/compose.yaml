services:
  transmission:
    image: lscr.io/linuxserver/transmission:latest
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:9091:9091"]
    volumes:
      - ${NOOK_DATA}/apps/transmission:/config
      - ${NOOK_DATA}/files/downloads:/downloads
    environment:
      TZ: ${TZ:-UTC}
