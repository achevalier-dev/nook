services:
  actualbudget:
    image: actualbudget/actual-server:26.5.2
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:15006:5006"]
    volumes:
      - ${NOOK_DATA}/apps/actualbudget/data:/data
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
