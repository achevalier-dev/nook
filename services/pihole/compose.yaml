services:
  pihole:
    image: pihole/pihole:2026.05.0
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8800:80"]
    volumes:
      - ${NOOK_DATA}/apps/pihole/etc/pihole/:/etc/pihole
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
