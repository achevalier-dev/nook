services:
  webdav:
    image: ugeek/webdav:amd64
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:5005:80"]
    volumes:
      - ${NOOK_DATA}/apps/webdav:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
