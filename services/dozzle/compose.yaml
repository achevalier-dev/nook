services:
  dozzle:
    image: amir20/dozzle:latest
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8888:8080"]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      TZ: ${TZ:-UTC}
