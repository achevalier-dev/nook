services:
  jellyfin:
    image: linuxserver/jellyfin:10.10.6
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8100:8096"]
    volumes:
      - ${NOOK_DATA}/apps/jellyfin-nvidia/config:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
