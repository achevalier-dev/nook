services:
  jdownloader2:
    image: jlesage/jdownloader-2:latest
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:5801:5800"]
    volumes:
      - ${NOOK_DATA}/apps/jdownloader2/config:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
