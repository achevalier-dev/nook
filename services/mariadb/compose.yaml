services:
  mariadb:
    image: linuxserver/mariadb:11.4.8
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:3306:3306"]
    volumes:
      - ${NOOK_DATA}/apps/mariadb/config:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
