services:
  metube:
    image: ghcr.io/alexta69/metube:latest
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8081:8081"]
    volumes:
      - ${NOOK_DATA}/apps/metube:/downloads
    environment:
      TZ: ${TZ:-UTC}
