services:
  2fauth:
    image: 2fauth/2fauth:6.1.3
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8001:8000"]
    volumes:
      - ${NOOK_DATA}/apps/2fauth/data:/2fauth
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
