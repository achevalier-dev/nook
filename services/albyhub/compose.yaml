services:
  albyhub:
    image: ghcr.io/getalby/hub:v1.22.2
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:58000:8080"]
    volumes:
      - ${NOOK_DATA}/apps/albyhub/data:/data
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
