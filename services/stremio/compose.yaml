services:
  stremio:
    image: tsaridas/stremio-docker:v1.3.4
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8100:8080"]
    volumes:
      - ${NOOK_DATA}/apps/stremio/config:/root/.stremio-server
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
