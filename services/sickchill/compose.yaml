services:
  sickchill:
    image: linuxserver/sickchill:2023.6.27
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:8082:8081"]
    volumes:
      - ${NOOK_DATA}/apps/sickchill/config:/config
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
