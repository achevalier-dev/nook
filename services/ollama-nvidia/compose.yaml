services:
  ollama:
    image: ollama/ollama:0.9.5
    restart: unless-stopped
    ports: ["${NOOK_TS_IP}:11435:11434"]
    volumes:
      - ${NOOK_DATA}/apps/ollama-nvidia/:/root/.ollama
    environment:
      TZ: ${TZ:-UTC}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
